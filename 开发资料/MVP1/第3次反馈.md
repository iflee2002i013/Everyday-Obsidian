## 月视图反馈
1. 日历列表的切换能不能做成一个按钮？（在列表状态点击一下切换日历状态那种的）
2. 当侧边栏太窄的时候，上方的功能按钮太长了，需要左右滑动才能操作。不如做一个按钮位置的自动换行。
## 其他反馈
1. YAML值还是改成summary，这个是我上次反馈时不小心敲错了。
2. 在第一次反馈中，“正文不再写入 `<!-- Everyday:start -->` / `<!-- Everyday:end -->` 标记，改为只维护 `## 一句话` 小节；旧标记会在下次保存该日记时自动清理。”这个功能现在已经没用了直接删除掉，我测试这个功能会误删日记正文内容，所以必须删除。
3. 我建议不用把一句话加到正文中了，就写到YAML中就好，这样你刚刚做的优先同步正文的“##一句话”功能也可以去掉了。化繁为简最好。

---

已按第三次反馈完成调整。

## 本轮执行结果

- 月视图顶部的“列表 / 日历”双按钮已改成单个切换按钮：
  - 当前是列表时，按钮显示“日历”，点击切到日历模式。
  - 当前是日历时，按钮显示“列表”，点击切回列表模式。
- 月视图顶部工具栏已改为可自动换行，侧边栏较窄时按钮会换到下一行，不需要横向滑动才能操作。
- YAML 字段已从误写的 `summery` 改回 `summary`。
  - 读取时仍兼容旧 `summery`，避免旧记录在保存迁移前丢失显示。
  - 保存时会删除旧 `summery`，统一写入 `summary`。
- 已删除正文管理逻辑：
  - 新建日记不再写入 `## 一句话` 小节。
  - 保存日记不再更新正文中的 `## 一句话` 小节。
  - 不再查找或清理 `<!-- Everyday:start -->` / `<!-- Everyday:end -->` 旧标记，避免误删正文。
- 月视图现在只从 YAML frontmatter 读取一句话内容。
- README 已同步更新为新的 YAML-only 设计。

已运行验证：`npm.cmd run build` 通过，`main.js` 已重新生成。

---
# 插件功能验证
为了进一步验证插件的月视图的功能，请你在Diary文件夹下再新建不同年份的若干日记内容，我来检查一下不同年份的日记识别效果。